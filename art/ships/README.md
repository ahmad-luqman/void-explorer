# AURORA VX-9 design references

`aurora-turnaround-v1.png` was generated with the built-in image tool from the preserved prompt in `aurora-turnaround-v1.prompt.md`, using the original flight sheet as a style reference.

The image establishes the visual design, not an exact CAD specification. The front and side views simplify overlapping wings and gear; the runtime model resolves those ambiguities with four separate planar wings, two vertical stabilizers, and three landing feet. No additional wingtip lights are placed on the fins.

The model preserves the simulation's 28.8 m wingspan and existing gear contact locations. Geometry is authored in Blender from a reproducible script, with an editable `.blend` and a compact GLB export. The runtime reference is the exported geometry and measured dimensions.
